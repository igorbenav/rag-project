# RAG over PDFs

A retrieval-augmented generation backend: ingest PDFs, ask questions, get cited
answers. FastAPI, the Mistral API, and no third-party search, RAG, or vector
database library — the index, the keyword search, and the ranking are all in
this repository.

Design notes and run instructions land here as the pipeline is built.
